apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-ingress
spec:
  ingressClassName: ingress-nginx
  tls:
  - hosts:
    - gitmoji.k3s.lan
    secretName: k3s-lan-tls
  rules:
  - host: gitmoji.k3s.lan
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: {{ .Release.Name }}
            port:
              name: http