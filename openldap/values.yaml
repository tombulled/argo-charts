replicaCount: 1

image:
  repository: osixia/openldap
  tag: "1.5.0"
  # repository: docker.io/bitnami/openldap
  # tag: 2.6.2
  pullPolicy: IfNotPresent

service:
  type: NodePort
  port: 30389
  targetPort: 389

customLdifFiles:
  0-initial-ous.ldif: |-
    dn: ou=People,dc=test,dc=k3s,dc=lan
    objectClass: organizationalUnit
    ou: People
    
    dn: ou=Group,dc=test,dc=k3s,dc=lan
    objectClass: organizationalUnit
    ou: Group